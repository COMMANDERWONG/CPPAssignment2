Extra Features:
Implemented "look xxx" command to enemies. For example, using "look zombie" will display the description of "zombie".
Used '>' as an indication to tell the player to enter a command.
A "quit" command which can be run at any moment during gameplay. It allows the player to quit the game. 
